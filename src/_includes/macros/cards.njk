{% macro person(student, env) %}
    {% set studentPath = (student.fnamn + '-' + student.enamn) | slug + '/' %}
    {% set studentSlug = (student.fnamn + '-' + student.enamn) | slug %}
    <div id="{{studentSlug}}" class="card | shadow-xs dark:shadow-s flow p-2xs rounded-s"  w:w="max-xl">
        {% if env == "production" %}
            {% image 'alster',
            studentPath + 'portrait.png',
            "Porträtt av {{student.fnamn}} {{student.enamn}}",
            studentSlug %}
        {% else %}
            <img src="/assets/alster/{{studentPath + 'portrait.png'}}" alt="">
        {% endif %}
        <h3>
            {{student.fnamn}}
            <br>
            {{student.enamn}}
        </h3>
        <a class="button" href="/student/{{studentSlug | slug}}">
            Läs mer
        </a>
    </div>
{% endmacro %}
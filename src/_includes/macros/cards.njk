{% macro person(student, env) %}
    {% set studentSlug = (student.fnamn + '-' + student.enamn) | slug %}
    <div id="{{studentSlug}}" class="card | shadow-xs dark:shadow-s flow p-2xs rounded-s">
        {% if env == "production" %}
            {% image 'alster',
            studentSlug + '/' + 'portrait.png',
            "Porträtt av {{student.fnamn}} {{student.enamn}}",
            studentSlug %}
        {% else %}
            <img src="/assets/alster/{{studentSlug + '/' + 'portrait.png'}}" alt="">
        {% endif %}
        <h3>
            {{student.fnamn}}
            <br>
            {{student.enamn}}
        </h3>
        <a w:text="xs" w:font="medium"  class="button" href="/student/{{studentSlug | slug}}">
            Läs mer
        </a>
    </div>
{% endmacro %}
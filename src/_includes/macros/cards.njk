{% macro person(student, env) %}
    {% set studentSlug = (student.fnamn + '-' + student.enamn) | slug %}
    <div w:border="dark:1 accent/10 rounded-[1px]" w:pos="relative" w:cursor="pointer sm:auto" id="{{studentSlug}}" class="card | shadow-xs dark:shadow-s p-2xs rounded-s">
        {% if env == "production" %}
            {% image 'alster',
            studentSlug + '/' + 'portrait.png',
            "Porträtt av {{student.fnamn}} {{student.enamn}}",
            studentSlug %}
        {% else %}
            <img src="/assets/alster/{{studentSlug + '/' + 'portrait.png'}}" alt="">
        {% endif %}
        <h3 w:m="t-[0.5em]">
            {{student.fnamn}}
            <br>
            {{student.enamn}}
        </h3>
        <a w:text="xs" w:font="medium" class="button secret" href="/student/{{studentSlug | slug}}">
            <span>Läs mer</span>
        </a>
    </div>
{% endmacro %}
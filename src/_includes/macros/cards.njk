{% macro person(student, env) %}
    <div class="card | shadow-xs flow p-2xs rounded-s"  w:w="max-xl">
        {% set studentPath = (student.fnamn + '-' + student.enamn + '/') | lower %}
        {% set studentSlug = (student.fnamn + '-' + student.enamn) | lower %}
        {% if env == "production" %}
            {% image 'alster',
            studentPath + 'portrait.png',
            "Porträtt av {{student.fnamn}} {{student.enamn}}" %}
        {% else %}
            <img src="/assets/alster/{{studentPath + 'portrait.png'}}" alt="">
        {% endif %}
        <h3>
            {{student.fnamn}}
            <br>
            {{student.enamn}}
        </h3>
        <a class="button" href="/student/{{studentSlug | slug}}">
            Läs mer
        </a>
    </div>
{% endmacro %}